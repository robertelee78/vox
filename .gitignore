# Ruflo / Claude-Flow config + runtime artifacts (local, not source)
.claude/
CLAUDE.md
.claude-flow/
ruvector.db
agentdb.rvf
agentdb.rvf.lock

# Rust build output (for when implementation begins)
/target/
**/*.rs.bk
